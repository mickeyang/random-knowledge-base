Central Ledger Log
Topic/Project: Peer review of R web-scraper for Kongfz (孔夫子旧书网) second-hand book monitor — `httr2` + JSON API → GT HTML report filtered by target discount rate
AI Tool Used: Yuanbao (元宝)
Status/Takeaway: Script is functional but has fragility in 5 areas to refactor before production/cron: (1) drop `mutate_all(as.character)` — kills typing, forces re-cast downstream and makes `filter(book_sold_out == "FALSE")` brittle; (2) wrap `req_perform()` in `tryCatch` or use `httr2::req_retry()` so one ISBN 403/429 doesn't abort entire run; (3) move hardcoded cookies (`shoppingCartSessionId`, `kfz_uuid`, `PHPSESSID` etc.) to `Sys.getenv()` via `.Renviron` — currently a security leak + short TTL on Kongfz sessions; (4) replace `for (i in 1:n)` + `[[i]]` with `purrr::pmap` for row-wise tibble iteration, and swap `Sys.sleep(3)` for `httr2::req_throttle(1/0.33)`; (5) build URLs *after* tibble construction (current `tibble(isbn, urls=str_glue(...), full_price, target_discount)` silently assumes equal-length vectors). Bonus: hitting `/keyword/list?keyword={isbn}` is a keyword search, not ISBN-resolved — risk of false-positive matches; check if Kongfz exposes an `/isbn/` endpoint. Next-level upgrade: wrap in `targets` DAG with `tar_cue(mode="always")` on scrape step for monitor-style periodic runs.
Direct Link: [not available — single-turn local context]